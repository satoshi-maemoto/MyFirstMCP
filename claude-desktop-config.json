{
  "mcpServers": {
    "my-first-mcp": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "myfirstmcp-mcp-server"
      ],
      "env": {
        "NODE_ENV": "production",
        "DOCKER_ENV": "true"
      },
      "description": "My First MCP Server with echo, time, and calculation tools"
    }
  }
} 